apiVersion: apps/v1
   kind: Deployment
   metadata:
     labels:
       app.kubernetes.io/name: klimavennelig-abc
     name: klimavennelig
   spec:
     replicas: 1
     selector:
       matchLabels:
         app.kubernetes.io/name: klimavennelig-abc
     template:
       metadata:
         labels:
           app.kubernetes.io/name: klimavennelig-abc
       spec:
         containers:
         - image: nginx:alpine
           name: nginx
           ports:
           - containerPort: 80
